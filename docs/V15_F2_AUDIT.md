# V1.5 F2 · QuickBooks IIF Export — Audit

**Shipped:** `quickbooks_export_jobs` table (company_id scoped, RLS on authenticated + service_role, three indexes on company/status/created_at). Pure IIF/CSV generator at `src/lib/quickbooks-export.ts` with CUST/VEND/TRNS/SPL/ENDTRNS blocks, tab-separated, CRLF endings, MM/DD/YYYY dates, 2-decimal amounts, tab/newline escape on memo fields. Server-only runner at `src/lib/quickbooks-runner.ts` that loads scoped `pedimento_facturas` by `company_id` + `invoice_date` range, uploads to Storage bucket `quickbooks-exports` at path `{company_id}/{YYYY}/{job_id}.{ext}`, writes status transitions (pending → running → ready | failed), and fires `interaction_events` row with `event_type='quickbooks_export_completed'` (server-side insert — does NOT widen the client-side `TelemetryEvent` union per plan). POST `/api/quickbooks/export` with Zod validation + role gate (contabilidad + admin + broker) creates the job and races the runner against a 10s timeout; GET `/api/quickbooks/export/[id]` returns status + signed 10-min download URL. `/admin/quickbooks-export` route renders AGUILA silver-glass form (date range + entity + format) with silver-gradient CTA and a 20-row recent-exports table scoped by `company_id`. Silver info banner surfaces the Fase 1 posture and — when the Storage bucket is missing — the row flips to `failed` with `error = 'bucket_missing'` so the UI shows "Bucket sin provisionar" gracefully. Nav: `/admin/quickbooks-export` added to `INTERNAL_GROUPS` (Administración) and `CONTABILIDAD_NAV`. CLAUDE.md V1 cockpit approved-routes list (Cross-domain) extended with the new route.

**Deferred:** Intuit Web Connector / Online API direct integration (pending Intuit developer certification); ACH bank-feed reconciliation; granular tax-line mapping (IVA/IEPS split into dedicated accounts); multi-currency exchange rate capture per transaction; polling UI (client currently prepends optimistic row — no background poller wired); custom per-client account mappings (currently hard-coded to `Ingresos:Servicios aduanales` / `Accounts Receivable`).

**Test delta:** 267 → 279 (+12 tests in `src/lib/__tests__/quickbooks-export.test.ts`): IIF structure (CUST, VEND, TRNS/SPL/ENDTRNS blocks), fracción embedding in SPL memo, MM/DD/YYYY + 2-decimal amount formatting, CRLF line endings, tab/CR/LF field escape, null handling, CSV header + comma-quoting, row count aggregation.

**Bucket provisioning needed (Throne):** Renato must create a private Supabase Storage bucket named `quickbooks-exports` before the first production run. Until then, the job row flips to `failed` with `error = 'bucket_missing'` and the UI surfaces "Bucket sin provisionar" — no silent failure, no partial upload. Migration is idempotent and safe to re-apply.
