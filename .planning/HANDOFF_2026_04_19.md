# Handoff — Sunday 2026-04-19 EOD → Monday 2026-04-20 ship

Three marathons (Thursday · Friday · Saturday · Sunday) culminating in the
Monday 8 AM credential send to Ursula Banda at EVCO Plastics.

## What's RUNNING

Production on `overnight/ursula-ready` (HEAD `e264cd4`):
- **Parte intelligence** — `/api/catalogo/partes` (list) + `/api/catalogo/partes/[cveProducto]` (detail) · session-scoped · service-role · SuperTito stats included
- **Catálogo list** — "Ver ficha →" 60px drilldown chip on every Partes-mode row
- **Parte detail page** — 4 tabs (Historia, Clasificación+OCA, Proveedores, Costos) · role gates · SVG cost chart
- **CRUZ wordmark** — silver gradient under gold Z on /login, 24px mobile / 32px desktop
- **Morning briefing infrastructure** — schema/generator/component/dismiss flow all built (Friday B5), PM2 entry in ecosystem.config.js (cron_restart `0 7 * * 1-5`)
- **Anthropic fallback** — chat route handles 3 error modes, briefing script fires Telegram alert on credit failure
- **Per-IP rate limit** on `/api/auth` (5/min, Saturday B5)
- **Regression audit** — `scripts/weekend-regression-audit.js` (Saturday B1)
- **RLS audit** — `scripts/rls-audit.js` · `scripts/verify-client-isolation.js` (Saturday B2)
- **Cold-start harness** — `scripts/test-cold-starts.js` (Friday B1)
- **Perf baseline** — `scripts/perf-baseline.js` + `scripts/lcp-inspector.js` (Saturday B3)

Both domains healthy (HTTP 200, sync.ok, supabase.ok). Last cold-start test: 1 cold + 4 warm, **0 false `/login?stale=1` redirects**. Last 5 dry-runs: **5/5 READY**. Last isolation probe: **12/12 PASS, 0 leaks**.

## What's BLOCKED (Sunday evening work for Renato)

Listed in execution order in `/tmp/monday-launch-runbook.md`. Summary:

1. **Apply two Supabase migrations** via SQL editor (5 min)
   - `supabase/migrations/20260417_client_briefings.sql` — creates `client_briefings` table + RLS
   - `supabase/migrations/20260417_parts_rls.sql` — enables RLS + indexes on 4 parts tables
   - Both idempotent. `scripts/parts-data-inventory.js` verifies migration 2 closed the anon-read leak
2. **Topup Anthropic credits** — currently insufficient. CRUZ AI + briefing both depend on this. Without: briefing renders null, chat shows "créditos agotados" Spanish fallback
3. **Register briefing cron on Throne**: `pm2 start ecosystem.config.js --only client-briefing-generator && pm2 save`
4. **Seed Monday briefing**: `node scripts/generate-client-briefing.js --company-id evco --date 2026-04-20 --force` — iterate Sonnet output until it reads natural (3 sentences, specific to EVCO's week)
5. **iPhone Safari walkthrough** (Monday 07:30) — real device, not Playwright Chromium

## What the NEXT session should do first

If Tito approved and Monday ship happened:
- `git log --oneline --since=yesterday` — confirm what landed Monday
- Check Monday briefing content: `SELECT briefing_text FROM client_briefings WHERE company_id='evco' ORDER BY generated_at DESC LIMIT 1;`
- Run `scripts/weekend-regression-audit.js` to spot any live regressions
- Start logging Ursula's observed behavior in `.claude/memory/observations.jsonl`

If Monday ship DIDN'T happen:
- Read `/tmp/cruz-sunday-ship-report.md` risk registry
- Check which of the 4 Sunday blockers didn't clear
- Don't proceed to Block N+1 code work — pick up the unblock sequence

## What NOT to do

- **Don't re-enable `pedimentos` in `/api/data` ALLOWED_TABLES** until the `20260417_pedimento_data.sql` migration (dated Apr 17) is applied. Current revert is intentional.
- **Don't re-introduce the most_used sort "pre-rank from partidas" fix** without a materialized view or an RPC. The `.in(cve_producto, pageCves)` approach returned 0 rows in prod — something about cve_producto values not matching across tables. Investigate before retrying.
- **Don't migrate `/pedimentos/nuevo` to /api/data** until you verify the RLS migration has applied. The page is broken either way; fixing it before RLS is pointless.
- **Don't add `portal_company_name` back to any SELECT** without also adding a migration that creates the column.

## Artifacts for handoff

- `/tmp/cruz-sunday-ship-report.md` — full Sunday ship report (3 deploys, all block summaries, risk registry)
- `/tmp/monday-launch-runbook.md` — copy-paste ready Sunday evening + Monday morning sequence
- `/tmp/tito-review-script.md` — 10-min Spanish walkthrough for Tito
- `/tmp/tito-telegram-sunday-eod.md` — Telegram draft (NOT sent; left for Renato)
- `/tmp/parts-data-reality.md` — EVCO data inventory
- `/tmp/weekend-regression-report.md` — last audit run
- `/tmp/rls-audit.md` — 54-table anon-read gap documentation
- `~/cruz-branch-backups/sunday-midday-*.bundle` · `sunday-post-features-*.bundle` · `sunday-eod-*.bundle`

## Rating trajectory

- Thursday EOD: 9.5/10 (regression triage)
- Friday EOD: 9.6/10 (8-block marathon — cold-start, taps, empty states, briefing infra)
- Saturday EOD: 9.3/10 (7-block hardening — RLS found, auth hardened, graceful fallbacks)
- **Sunday EOD: 9.5/10** (9-block features — parte intelligence, logo, 3 deploys)

Pattern: features push up, external deps push down. Monday 10/10 requires Renato's 3 Sunday manual steps to land.

## What matters for Ursula specifically

She's a plant manager at EVCO Plastics. What she needs Monday morning:

1. Login works
2. She can see her active shipments (there are 0 today — quiet season — the portal handles this with calm copy)
3. She can tap into Catálogo → see her 148K products → drill into any one → see classification history, OCAs, suppliers, costs
4. She can ask CRUZ AI a simple question and get a real answer in Spanish

The rest is infrastructure. If those 4 work, the ship is good.
