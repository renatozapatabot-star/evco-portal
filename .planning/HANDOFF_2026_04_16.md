# Handoff — 2026-04-16

## Where the platform is

Live at **portal.renatozapata.com** (CNAME → Vercel) with backup alias
`evco-portal.vercel.app`. Last prod deploy: today, client cockpit polish.
Rating 9.8/10. Tests 343/343 green. Branch
`feature/embarques-rename-month-scope`, last commit `11ad6d2`.

---

## What shipped this session

- Removed floating **Asistente ZAPATA AI** + **Clasificador** buttons
  from the client cockpit (`/inicio` only — operator/owner surfaces
  unchanged). `AsistenteButton` import + render removed in
  `src/app/inicio/page.tsx`.
- Removed **"Mis embarques activos"** panel (`<ClienteEstado>`) and
  **"Actividad reciente"** right rail from the client cockpit.
  `CockpitInicio` (`src/components/aguila/CockpitInicio.tsx`) now
  skips the entire 2-col main block when both `estadoSections` and
  `actividadSlot`/`actividad` are absent — operator/owner surfaces are
  unaffected because they still pass those slots.
- Reworked **Expedientes** + **Catálogo** nav tiles on `/inicio`:
  - Headline number is now this-month activity (documents uploaded
    this month / parts classified this month)
  - Lifetime totals (was 214,544 / 148,537) demoted to microStatus
    subtitle: "Documentos este mes · N en total" / "Partes usadas
    este mes · N en catálogo"
  - Two new soft-counted Supabase queries added:
    `expediente_documentos.uploaded_at >= monthStart` and
    `globalpc_productos.fraccion_classified_at >= monthStart`
- Deployed via `vercel --prod` to alias `evco-portal.vercel.app`.

Diff is in `git status` — three files touched: `CLAUDE.md`,
`src/app/inicio/page.tsx`, `src/components/aguila/CockpitInicio.tsx`.
All four changes were prompted by Renato's live-portal screenshots
showing the client cockpit looked busy and overwhelming.

---

## What's running right now

**Cron (Throne):**
- `nightly-pipeline.js` @ 1 AM — globalpc sync → completeness check →
  anomaly + solicit
- `econta-reconciler.js` @ Mon 4 AM — receivables match against
  completed tráficos

**PM2 processes** (verify with `pm2 status` on Throne):
- `cruz-bot`
- `fold-agent`
- `workflow-processor`

**Supabase mirror tables (hot, syncing nightly):**
- 8 `globalpc_*` tables (productos 315K+, partidas, facturas, eventos,
  contenedores, ordenes_carga, bultos, proveedores)
- 4 `econta_*` tables (facturas, cartera, ingresos, egresos)

**Vercel:** prod alias on `evco-portal.vercel.app` and
`portal.renatozapata.com`.

---

## What's pending

- **GlobalPC + eConta workflow audit** — Renato signaled this is coming.
  When delivered, capture each workflow into the gap matrix using the
  Audit Capture Framework below.
- **3-credential recon** — Arturo (AduanaNet) + Anabel (eConta) + Tito
  (GlobalPC admin). Marathon 3 cannot be scoped until this happens.
- **eConta MySQL writer (PM2)** — IIF generator at
  `src/lib/quickbooks-export.ts` is ready. Queue table
  `trafico_econta_exports` exists. Endpoint at
  `src/app/api/econta/export/route.ts` queues writes. The PM2 script
  that drains the queue into eConta MySQL is NOT deployed. Wait for
  Anabel's workflow recon before shipping.
- **WSDL document pull** — `wsdl-document-pull.js` exists but isn't in
  cron. Decide phase after audit.
- **Tito walkthrough** — V1 portal has not yet been shown to Tito.
  Pending his availability.

---

## Known issues

- 3 legacy dark-theme PDFs in `app/api/` (auditoria-pdf, reportes-pdf,
  anexo24-pdf) — not yet repainted to V1 cinematic glass
- `/contabilidad/exportar` and `/contabilidad/kpis` are placeholders
- Client A/R visibility into own `econta_*` data is broker-internal
  today (deliberate design decision per Mensajería boundary)
- `TELEGRAM_SILENT` flag state should be verified before re-enabling
  alerts — was muted with smart thresholds in place

---

## What to do first next session

1. `git status && git log -1 --oneline` — confirm whether the cockpit
   changes from 2026-04-16 are committed (deployed but possibly
   uncommitted on `feature/embarques-rename-month-scope`)
2. `bash scripts/gsd-verify.sh` — verify nothing drifted
3. SSH Throne and run `pm2 status` — confirm cruz-bot,
   workflow-processor, fold-agent are all up
4. **If Renato has delivered the audit:** open the Audit Capture
   Framework below, work through each workflow, build the gap matrix
5. **If not:** pick from "What's pending" above and ask Renato priority

---

## Audit Capture Framework (when Renato delivers GlobalPC + eConta audit)

Capture each workflow as a structured entry:

```
### [Workflow name, e.g. "Tito approves a pedimento draft"]

- **Who:** Tito / Arturo / Anabel / Juan José / Eloisa
- **System:** GlobalPC · eConta · AduaNet · external
- **Frequency:** per tráfico · daily · weekly · monthly · on event
- **Steps today:** 1… 2… 3…
- **Inputs:** fields used, sources, who provides them
- **Outputs:** what downstream systems consume the result
- **Approval chain:** who signs off, what triggers approval
- **Friction today:** why this is tedious or error-prone
- **ZAPATA AI status:** ✅ fully / 🟡 partial / ❌ missing
- **Gap to close:** specific (table, field, route, script)
- **Cutover risk:** low / medium / high — and why
- **Dependencies:** other workflows that must migrate first
```

After all workflows are logged, produce the Integration Gap Matrix:

| Workflow | Owner | System | Status | Effort (S/M/L) | Risk | Phase |
|----------|-------|--------|--------|----------------|------|-------|

**Tentative Marathon 3 phasing** (confirm with Renato before scoping):

- **3A — Read parity:** every field Tito/Anabel currently *looks at*
  in GlobalPC/eConta is visible inside ZAPATA AI without opening those
  systems
- **3B — Capture parity:** every field they currently *type into*
  GlobalPC/eConta has a ZAPATA AI form that writes forward
- **3C — Approval parity:** Tito's sign-off flows execute entirely
  inside ZAPATA AI with `audit_log` immutability (SAT-safe chain)
- **3D — Export parity:** QB IIF writer deployed, Anabel's monthly
  close runs from ZAPATA AI
- **3E — Quiet retirement:** two-week parallel ops window, then
  GlobalPC + eConta credentials move to archive-only mode (Throne IP
  whitelist not revoked — kept as safety net)

---

## Critical files to reference during audit capture

**Data model:**
- `types/supabase.ts:5692-6099` — globalpc_* column definitions
- `supabase/migrations/20260504_econta_pl_tables.sql` — eConta schema
- `supabase/migrations/20260510_econta_export_queue.sql` — export queue
- `supabase/migrations/20260417_missing_gateway_tables.sql` — recent
  gateway additions

**Sync layer:**
- `scripts/globalpc-sync.js` — the nightly mirror
- `scripts/full-sync-econta.js` — eConta tables, extend `NEW_TABLES` here
- `scripts/econta-reconciler.js` — Mon 4 AM CxC match
- `scripts/historical-sync.js` — multi-client backfill
- `scripts/nightly-pipeline.js` — cron orchestrator

**App surfaces:**
- `src/app/contabilidad/inicio/page.tsx` — Anabel's cockpit (extend here)
- `src/app/operador/contabilidad/page.tsx` — operator accounting view
- `src/lib/contabilidad/aging.ts` — CxC aging (reuse, don't duplicate)
- `src/lib/quickbooks-export.ts` — IIF generator (ready, needs writer)
- `src/lib/aguila/tools.ts` — AI context (extend with eConta reads)

**Integration:**
- `src/app/api/econta/export/route.ts` — queue endpoint (writer pair pending)
- `src/lib/api-schemas.ts` — `ALLOWED_TABLES` gate

---

## Open framing questions for Renato (before audit starts)

1. **Audit format:** screen share live, you type it out, screen recording,
   or you narrate while I ask follow-ups?
2. **Cutover posture:** hard cutover by May 15, soft 2-week parallel,
   or deeper integration with no May commit?
3. **Role lens:** whose workflow anchors the audit — Tito's, Anabel's,
   Arturo's, Juan José + Eloisa's, or all of them in sequence?
4. **GlobalPC write-back posture:** Mario constraint — confirmed
   permanent, or is there a path to bidirectional during the cutover
   window?
