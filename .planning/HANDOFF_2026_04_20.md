# HANDOFF — Monday 2026-04-20 · 10/10 push, all-week posture

Session owner: Renato Zapata IV via autonomous delegation.
Branch: `fix/pdf-react-pdf-literal-colors-2026-04-19`.
Head commit: `94a19de chore(lint): clean trailing whitespace from eslint --fix`.

**Posture shift:** the Sunday HANDOFF was written under "Monday 08:00 CT
ship pressure." Renato redefined scope this morning as "no launch — all
week to get it perfect and a 10/10." Session posture is now quality-over-
speed.

---

## Commits landed this session (9 on top of the Sunday night 7)

### Sunday night (pre-wake pass · committed 23:00–07:05 CT)
1. `5b109c4` — 24 lint errors cleared (ship gate 1 unblocked)
2. `673c772` — `aging.test.ts` tenant-isolation contract (8 assertions)
3. `d6746db` — `HANDOFF_2026_04_19_night.md`
4. `64af0b7` — R11 + R12 ratchets (scripts/ rates + `/mi-cuenta` tone)
5. `66ee925` — `CRON_MANIFEST.md` (28 PM2 processes)
6. `9fa236c` — R13 silent-catch ratchet (baseline 161)
7. `91c0c18` — `ship.sh` gate 1 now includes `npm run lint`

### Monday morning (post-wake quality push · 07:17 CT onward)
8. `74b67db` — **rate hardcode sweep, 4 files** · R11 ratcheted 4 → 0
   - `generate-invoice.js` fetches `getIVARate()` at main() with
     Telegram SEV-1 + `process.exit(1)` on failure
   - `invoice-handlers.js` removed silent try/catch fallback to 0.16,
     now returns `{success: false}` + Telegram alert
   - `cruz-mcp-server.js` removed `?? IVA_RATE / ?? 0.008 / ?? 17.5`
     silent fallbacks; `estimateLandedCost` returns
     `{error: 'rates_unavailable'}` on stale config
   - `cost-optimizer.js` fxSavingsPct → system_config
     `fx_savings_heuristic_pct` with graceful skip
   - **Pending migration:** `20260420_fx_savings_heuristic.sql`
9. `bb2e6ca` — **silent-catch sweep, 3 cron-critical scripts** · R13 161 → 155
   - po-predictor · workflow-processor · cost-optimizer all replace
     inline Telegram helpers with shared `scripts/lib/telegram.js`
   - Error-path swallows replaced with structured `console.warn`
   - CLAUDE.md's "po-predictor known bugs" confirmed STALE (pagination
     fixed via `.range()`; main().catch correctly exits 1)
10. `d951251` — **perf indexes migration** (4 compound indexes)
    - `expediente_documentos (company_id, uploaded_at DESC)`
    - `globalpc_productos (company_id, cve_producto)`
    - `globalpc_partidas (company_id, cve_producto)` + `(cve_producto)`
      — partidas had ZERO indexes prior
    - **Pending migration:** `20260420_perf_indexes.sql`
11. `83cc75f` — **system_config expiry-watch cron** (Tier 3.3)
    - New PM2 entry `system-config-expiry-watch` at `15 7 * * *`
    - 🔴 SEV-2 on expired · 🟡 on ≤3d · 🟢 on ≤7d
    - CRON_MANIFEST.md updated to 29 PM2 processes
12. `9afa4a1` — expiry-watch: `--verbose` mode + NULL `valid_to`
    detection for critical keys
13. `3b7f9d9` — **`/mi-cuenta/__tests__/isolation.test.ts`** (20 assertions)
    - Ethics contract SEV-2 fence per client-accounting-ethics.md §7
    - Refactored page.tsx to use pure `resolveMiCuentaAccess(...)` in
      `./access.ts` — testable authorization logic separated from SSR
14. `8cc7a67` — `supabase/MIGRATION_QUEUE.md`
    - Handoff doc for 2 pending migrations with apply/verify/rollback
15. `db28c89` — dead-code sweep · 392 → 387 warnings
16. `94a19de` — trailing whitespace cleanup from eslint --fix

---

## State at handoff

| Gate | Status |
|---|---|
| `npx tsc --noEmit` | 0 errors |
| `npm run lint` | 0 errors · 387 warnings (was 392 Sunday night) |
| `npx vitest run` | 97 files / **798 tests** (was 667 Sunday, +131 session) |
| `bash scripts/gsd-verify.sh --ratchets-only` | 0 failures · 19 warnings |
| `npm run build` | succeeds, 204 routes |
| `bash scripts/ship.sh --skip-deploy` | gates 1–3 green |

**Ratchets enforced:**
- R11 scripts/ rate hardcodes — **baseline 0** (enforced — new hardcode = fail)
- R12 `/mi-cuenta` calm tone — **baseline 0** (hard invariant)
- R13 scripts/ silent-catches — **baseline 155** (was 161, still target ↓)

**Production state (verified 07:17 CT):**
- `portal.renatozapata.com/` HTTP 307 / 1.13s
- `/api/health/data-integrity?tenant=evco` all tables green
- `/login` HTTP 200 / 0.42s
- `evco-portal.vercel.app/` HTTP 307 / 0.23s
- Production running `3ef4dca` (Block EE ship 2026-04-17) — tonight's
  branch NOT pushed

---

## Critical pending action

**system_config rates expire 2026-05-05** (15 days from today).
`banxico_exchange_rate`, `dta_rates`, `iva_rate`, `eta_model` all have
valid_to 2026-05-05. The new expiry-watch cron will fire:
- 🟢 heads-up on 2026-04-28 (7 days out)
- 🟡 urgent on 2026-05-02 (3 days out)
- 🔴 SEV-2 on 2026-05-05 (expired; pipelines refuse-to-calculate)

Broker action before 2026-05-05: refresh these rows' value + valid_to
via SQL on Supabase Studio. Pattern:

```sql
UPDATE system_config
   SET value = '{"rate": <new>}'::jsonb,
       valid_to = '2027-01-01'
 WHERE key = '<key>';
```

---

## Deploy sequence (when ready — Renato's call)

```bash
cd ~/evco-portal
git status                                 # confirm clean
bash scripts/ship.sh --skip-deploy         # full dry-run

# apply pending migrations (2):
npx supabase db push
# see supabase/MIGRATION_QUEUE.md for verification SQL

# regen types after migration:
npx supabase gen types typescript --linked > types/supabase.ts
git add types/supabase.ts
git commit -m "chore(types): regen after 20260420 migrations"

# deploy:
bash scripts/ship.sh                       # gates 1-6 incl Vercel push
# then on Throne:
#   pm2 reload ecosystem.config.js && pm2 save
#   # new process: system-config-expiry-watch

# audit:
# /audit 1 in Claude-in-Chrome at portal.renatozapata.com
# verify: /mi-cuenta feature-flag state (Vercel env
#   NEXT_PUBLIC_MI_CUENTA_ENABLED per Tito approval)
```

---

## Tier 2 still open (ordered by leverage)

1. **`any` triage continuation** — 387 → target < 100. Biggest remaining
   noise. Most are `no-unused-vars` (250) and `react-hooks/*` (100+).
   Underscore-prefix parameter convention handles ~half. React 19 hook
   rules need per-file review.
2. **More silent-catch sweeps** — 155 → target < 50. Non-cron scripts:
   network-intelligence (9), exception-detective (4), aduanet-puppeteer-
   scraper (4), lib/safe-write (3), demand-predictor (3).
3. **RLS red-team test suite** (Tier 3.7) — MAFESA white-label gate.
   Simulate cross-tenant sessions, assert data isolation at API level.
4. **Materialized view for `/inicio` aggregations** (Tier 3.2) — FCP
   target drop from ~800ms to ~200ms.
5. **Lighthouse CI** (Tier 3.6) — enforce perf budgets on every Vercel
   preview.
6. **Telegram alert coverage audit** (Tier 3.4) — build
   `scripts/alert-coverage-report.js` listing which of the 29 PM2
   scripts have wired alerts vs. which don't.

## Tier 4 (waiting on external)

- MAFESA dry-run — Tito: RFC + GlobalPC clave
- Anthropic credit topup — CRUZ AI features
- SAT RFC API creds — `SAT_RFC_API_URL` / `SAT_RFC_API_KEY`
- WSDL Formato 53 method name — Mario @ GlobalPC confirmation
- eConta MySQL writer — Anabel credential recon

---

## Structural changes added this session

Future sessions benefit from these permanently:

- `scripts/lib/telegram.js` is now the canonical Telegram sender — 3
  scripts migrated off inline helpers in `bb2e6ca`; pattern established.
- `scripts/gsd-verify.sh` has 3 new ratchets (R11, R12, R13) and lint
  is now in ship.sh gate 1. Every commit ratchets forward from here.
- `supabase/MIGRATION_QUEUE.md` is the new canonical handoff for
  pending DB changes — update alongside every new migration.
- `scripts/CRON_MANIFEST.md` documents all 29 PM2 processes with
  alert coverage flags.
- `/mi-cuenta` authorization is now in `./access.ts` (pure resolver),
  tested by `__tests__/isolation.test.ts` — the ethics contract's
  promised SEV-2 fence now exists.

---

## Appended session log · 08:00–08:20 CT

7 additional commits after the mid-session HANDOFF above:

| # | Commit | Impact |
|---|---|---|
| 18 | `2018027` | **`alert-coverage-audit.js`** — heuristic scanner of the 29 PM2 scripts (4 signals: Telegram, exit1, log, topCatch) |
| 19 | `65d7d67` | closed 2 real gaps: `seed-tariff-rates` (1/4 → 3/4) + `mensajeria-email-fallback` (2/4 → 3/4) |
| 20 | `f74ffb4` | heuristic refinement (notify alias + IIFE .catch) + `client-briefing-generator` (2/4 → 4/4) · **28 of 29 ≥ 3/4** |
| 21 | `5c390fd` | wired `alert-coverage-audit` into `ship.sh` gate 1 · **baseline 28 enforced non-regression** |
| 22 | `6b93c81` | **`rates.contract.test.ts`** — 11 assertions on refuse-to-calculate for `getIVARate` / `getDTARates` / `getExchangeRate` |
| 23 | `ec03114` | **`/api/catalogo/partes/[cveProducto]/__tests__/tenant-isolation.test.ts`** — 8 assertions locking cross-tenant → 404 not 403 (tenant-isolation.md invariant) |
| 24 | `e5b0e47` | **`MAFESA_ONBOARDING.md`** — one-page 10-step runbook consolidating every white-label precondition |

**Updated totals (end of continued session):**
- Tests **667 → 817** (+150 this full session; +19 in this appended block)
- Lint warnings **392 → 387**
- Alert coverage **17 full · 11 covered · 1 partial · 0 silent** (was silent 0 silent all along but 14 scripts were heuristic-false-negatives)
- `ship.sh` now enforces 5 quality gates at step 1/6: typecheck · lint · vitest · build · gsd-verify · block-audit · alert-coverage
- New permanent documentation: `HANDOFF_2026_04_19_night.md` · `HANDOFF_2026_04_20.md` · `CRON_MANIFEST.md` · `MIGRATION_QUEUE.md` · `MAFESA_ONBOARDING.md`

---

*Patente 3596 · Aduana 240 · Laredo, Texas · Est. 1941*
