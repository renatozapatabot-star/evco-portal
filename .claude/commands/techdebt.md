Scan the codebase for:
1. Any hardcoded 'evco' or 'mafesa' in src/app/
2. Any `any` or `unknown` TypeScript types
3. Any unused imports or dead code
4. Any console.log that should be removed
5. Any duplicate component logic to extract
Report findings. Do not fix — just report.
