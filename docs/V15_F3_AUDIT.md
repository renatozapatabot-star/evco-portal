# V1.5 F3 · Anabel accounting dashboard — audit

**Shipped.** `/contabilidad` cockpit with 6 silver-glass tiles: AR aging (`invoices`), AP aging (`aduanet_facturas` via `companies.clave_cliente`), monthly close checklist (8 seed items, idempotent upsert in `lib/contabilidad/close.ts`, optimistic toggle UI), MVE open-alert counts by severity, top-5 facturas ready queue, and last QuickBooks export status. New migration `monthly_close_checklist` table with `company_id` scoping, RLS (read + update own company, service role all), unique constraint `(company_id, month, item_key)`, and `(company_id, month)` index. New `GET /api/contabilidad/overview` + `POST /api/contabilidad/checklist/[id]/toggle` endpoints, both role-gated to `contabilidad|admin|broker` via `verifySession`. `CONTABILIDAD_NAV` anchored to `/contabilidad`; middleware root redirect for role `contabilidad` now points to `/contabilidad`. Server-side telemetry `metadata.event = 'contabilidad_overview_viewed'` fires on page mount (and on overview API hit) — `TelemetryEvent` union stays locked at 15.

**Deferred.** Dedicated AP source table (currently piggy-backs on legacy `aduanet_facturas`; UI renders graceful "Datos pendientes" tile when the clave lookup fails). Proper IVA calculation (only the checklist item is present; no accrual engine). Bank reconciliation (checklist-only). Per-row export to CSV from AR/AP tiles. Cliente-name resolution on AR top-debtors (today shows `invoice_number`).

**Test delta.** 279 → 279 (no regressions; new lib is pure data-shaping + idempotent upserts — matches F1/F2 precedent of deferring vitest coverage to later bundles). `typecheck` clean. `build` green. `gsd-verify` shows no new hex or lint violations from F3 files; pre-existing demo-page hex and ESLint errors are unchanged.
